# Secrets and personal branding — NEVER share these. Templates ship instead:
# .env.example and brand.example.json.
.env
brand.json

# Personal skill doc en la RAÍZ (tu @, credenciales, marcas, IDs de Metricool).
# No viaja. Anclado a "/" para NO matchear los SKILL.md de .claude/skills/**,
# que sí deben viajar.
/SKILL.md

# Generated / local
.venv/
__pycache__/
*.pyc
node_modules/
out/
.cache/
factory.db
assets/generated/
assets/lead-magnets/
remotion/public/generated/

# Session / harness artifacts (not project source) — EXCEPT the project's own
# skills, which travel with the repo so any Claude Code session in this folder
# inherits them.
.claude/*
!.claude/skills/

# Briefs — artefactos EFÍMEROS de revisión (uno por pedido, para que Alexander
# lea/corrija/apruebe antes de construir). Son regenerables y personales a su
# ciclo de aprobación; lo durable/compartible es el examples/<slug>.json que sí
# viaja. Mismo criterio que out/.
briefs/
