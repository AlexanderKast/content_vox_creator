# Only needed for factory/chroma.py (green-screen removal). Everything else
# in factory/ is deliberately stdlib-only.
numpy
Pillow

# factory/align.py — local word-level alignment of the ElevenLabs voice
# track (faster-whisper runs the Whisper model on CPU, no network call, no
# per-run cost; see the module docstring for why this is never the paid API).
faster-whisper

# factory/redact.py — local face/eye detection (Haar cascade, bundled with
# opencv, no network call, no per-run cost) to black-bar a public figure's
# eyes before a real scraped photo is ever cached or rendered. Headless
# build: no GUI deps, this repo never opens a window. Pinned — 5.0.0.93
# ships an empty cv2/data/ (no haarcascade XMLs, no CascadeClassifier);
# 4.10.0.84 is the last verified-good release with both.
opencv-python-headless==4.10.0.84

# factory/redact.py — remove_background(). Local u2net model (downloads once
# to ~/.u2net/, ~176MB, then free/offline) to isolate a public figure into a
# real cutout instead of a rectangular photo. No per-run cost, no API key.
rembg
